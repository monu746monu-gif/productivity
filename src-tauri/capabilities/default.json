{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the Vexa windows",
  "windows": ["main", "launcher"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
